"""
Per-block execution from parsed CSV rows. Implemented in a later phase.
"""


def execute_block_stub(block_type: str, block_id: str) -> tuple[str, str]:
    """Placeholder until executor is implemented."""
    return "pending", f"block {block_id} ({block_type}) not executed yet"
